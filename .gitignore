node_modules/
dist/
# EXCEPT the plugin's bundled collector — it IS the shipped runtime (colleagues
# run it via /plugin, no build step), so it must be committed here.
!cc-usage/dist/
!cc-usage/dist/**
.next/
out/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# os
.DS_Store

# local data dumps
*.local.json

# private dev docs — kept local, not published to GitHub
docs/superpowers/

# private — kept local, not published
PLANS.md
.claude/

# never commit local-only overrides
*.local.ts
